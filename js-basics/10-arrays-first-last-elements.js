const array= ["Mouse", "Laptop", "Computer", "Phone", "Camera"];

//first
console.log(array[0]);

//last
//console.log(array[3]); //hard coding for this is not recommended
console.log(array[array.length - 1]); //dynamic way to find the last element in an array, will always work